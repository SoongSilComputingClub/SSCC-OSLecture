#include <vellum/shell.h>

#include <stdio.h>

long VlShell_Readline(const char *__restrict prompt, char *__restrict buf, long len)
{
    /*
     * TODO(session 08-3):
     * Print the prompt, read characters from stdin, echo them, handle backspace,
     * and stop on newline.
     */
    (void)prompt;
    (void)buf;
    (void)len;
    return 0;
}
