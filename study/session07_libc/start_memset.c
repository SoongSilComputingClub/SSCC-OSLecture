#include <string.h>

void *memset(void *dest, int c, size_t count)
{
    /*
     * TODO(session 07-1):
     * Fill `count` bytes at `dest` with byte value `c` and return `dest`.
     */
    (void)dest;
    (void)c;
    (void)count;
    return NULL;
}
