#include <string.h>

void *memmove(void *dest, const void *src, size_t len)
{
    /*
     * TODO(session 07-3):
     * Implement overlap-safe byte copying for `memmove`.
     */
    (void)dest;
    (void)src;
    (void)len;
    return NULL;
}
